<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Authentication Complete</title>

    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Bungee&display=swap" rel="stylesheet">
    <style>
        body, html {
            background: #212121;
            text-align: center;
            color: #FFFFFF;
            font-family: 'Roboto', sans-serif;
        }
        *, *:before, *:after {
            box-sizing: border-box;
        }
        header {
            margin-top: 10%;
            user-select: none;
        }
        .logo-text {
            font-size: 72px;
            font-family: 'Bungee', 'Roboto', sans-serif;
        }
        .logo-blue {
            color: #0069C0;
        }
        .main {
            margin-top: 2.5%;
            font-weight: bold;
        }
        .main-text {
            font-size: 32px;
            user-select: none;
        }
        .token {
            margin-top: 2.5%;
        }
        .flow-input {
            position: relative;
            display: flex;
            flex-direction: row;
            width: 100%;
            max-width: 400px;
            margin: 0 auto;
            border-radius: 2px;
            padding: 0.4rem 1rem 0.6rem;
            background: #FFF;
        }
        .flow-input:after {
            content: "";
            position: absolute;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 999;
            height: 2px;
            border-bottom-left-radius: 2px;
            border-bottom-right-radius: 2px;
            background: #0069C0;
            background-size: 500% auto;
            animation: gradient 3s linear infinite;
        }

        .flow-input input {
            flex-grow: 1;
            color: #212121;
            font-size: 1.8rem;
            line-height: 2.4rem;
            vertical-align: middle;
            width: 93%;
            padding-right: 2%;
        }
        .flow-input button::-webkit-input-placeholder {
            color: #7881A1;
        }

        .flow-input button {
            color: #7881A1;
            font-size: 2.4rem;
            line-height: 2.4rem;
            vertical-align: middle;
            transition: color .25s;
        }
        .flow-input button:hover {
            color: #BFD2FF;
        }
        .container {
            display: flex;
            flex-direction: column;
            height: 100vh;
            max-width: 1600px;
            padding: 15px 15px;
            margin: 2.5% auto 0;
        }
        button { padding: 0; background: none; border: none; outline: none; }
        input { border-style: none; background: transparent; outline: none; }
    </style>
</head>
<body>
<header>
    <span class="logo-text">OMORI</span>
    <span class="logo-text logo-blue">CLOUD</span>
</header>
<div class="main">
    <span class="main-text">Welcome <%= tag %></span>
</div>
<div class="container">
    <div class="flow-input">
        <input id="copy-input" value="<%= token %>" type="text" readonly />
        <button id="copy" type="submit" style="fill: #212121;padding-top: 9px;padding-left: 5px;">
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24"><path d="M16 1H4c-1.1 0-2 .9-2 2v14h2V3h12V1zm3 4H8c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h11c1.1 0 2-.9 2-2V7c0-1.1-.9-2-2-2zm0 16H8V7h11v14z"/></svg>
        </button>
    </div>
</div>
<script>
  document.getElementById('copy').onclick = () => {
    const copyInput = document.getElementById("copy-input");

    copyInput.focus();
    copyInput.select();

    document.execCommand('copy');
  };
</script>
</body>
</html>